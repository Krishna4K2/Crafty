# ====================================================
# Crafty Microservices - Environment Variables
# ====================================================
# Copy this file to .env and update values as needed
# DO NOT commit .env file to GitHub - it contains sensitive data

# ====================================================
# Global Service Configuration
# ====================================================
NODE_ENV=production
APP_VERSION=1.0.0

# ====================================================
# Catalogue Service Configuration
# ====================================================
# Data source: json (default) or db (PostgreSQL)
DATA_SOURCE=json

# PostgreSQL Database Configuration (used when DATA_SOURCE=db)
DB_HOST=catalogue-db
DB_NAME=catalogue
DB_USER=user
DB_PASSWORD=password
DB_PORT=5432

# ====================================================
# Voting Service Configuration
# ====================================================
# Database profile: default (H2) or mongodb (MongoDB)
SPRING_PROFILES_ACTIVE=default

# MongoDB Configuration (used when SPRING_PROFILES_ACTIVE=mongodb)
SPRING_DATA_MONGODB_URI=mongodb://voting-db:27017/votingdb
MONGODB_DATABASE=votingdb

# Catalogue Service URL for Voting Service
CATALOGUE_SERVICE_URL=http://catalogue:5000/api/products

# Voting Service Port
SERVER_PORT=8086

# ====================================================
# Frontend Service Configuration
# ====================================================
# Service URLs
PRODUCTS_API_BASE_URI=http://catalogue:5000
RECOMMENDATION_BASE_URI=http://recommendation:8080
VOTING_BASE_URI=http://voting:8086

# Frontend Port
FRONTEND_PORT=3000

# ====================================================
# Recommendation Service Configuration
# ====================================================
# Catalogue Service URL for Recommendation Service
CATALOGUE_SERVICE_URL_RECOMMENDATION=http://catalogue:5000

# Recommendation Service Port
RECOMMENDATION_PORT=8080

# ====================================================
# Database Default Credentials
# ====================================================
# WARNING: Change these passwords for production!
POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_DB=catalogue

MONGO_INITDB_DATABASE=votingdb

# ====================================================
# Development/Testing Configuration
# ====================================================
# Uncomment these for development
# NODE_ENV=development
# DATA_SOURCE=json
# SPRING_PROFILES_ACTIVE=default

# ====================================================
# Production Configuration Example
# ====================================================
# For production, use these settings:
# NODE_ENV=production
# DATA_SOURCE=db
# SPRING_PROFILES_ACTIVE=mongodb
# POSTGRES_PASSWORD=your-secure-password
# Change other passwords as well!
