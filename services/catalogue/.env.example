# Catalogue Service Configuration
APP_VERSION=1.0.0

# Data Source Configuration
# Choose 'json' for file-based storage or 'db' for PostgreSQL database
DATA_SOURCE=json

# PostgreSQL Database Configuration
# These settings are used only when DATA_SOURCE=db
DB_HOST=catalogue-db
DB_NAME=catalogue
DB_USER=crafty
DB_PASSWORD=crafty

# Port Configuration
CATALOGUE_PORT=5000
POSTGRES_PORT=5432
